# This file populates the Overview on Charmhub.

# The charm package name, no spaces (required)
# See https://juju.is/docs/sdk/naming#heading--naming-charms for guidance.
name: hpct-ldap-server
display-name: hpct-ldap-server
description: OpenLDAP server operator for HPC operations.
summary: This operator is responsible for the OpenLDAP server and replicas.

peers:
  replicas:
    interface: ldap-replicas
provides:
  tls-cert:
    interface: ldap-tls